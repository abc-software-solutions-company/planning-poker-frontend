const PREFIX = '';

export const API_ENDPOINTS = {
  AUTH: `${PREFIX}/auth`,
  USER: `${PREFIX}/users`,
  ROOM: `${PREFIX}/rooms`,
  STORY: `${PREFIX}/stories`,
  USERROOM: `${PREFIX}/userRooms`,
  USERSTORY: `${PREFIX}/userStories`
};
